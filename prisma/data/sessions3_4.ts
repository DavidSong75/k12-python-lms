import { SessionData } from "./sessions1_2";

export const session3: SessionData = {
    number: 3,
    title: "연산자와 수식",
    description: "산술, 비교, 논리 연산자를 배우고 계산 프로그램을 만듭니다.",
    lessons: [
        { order: 1, title: "산술 연산자 복습", type: "lesson", content: `# ➕ 산술 연산자 복습\n\n## 기본 산술 연산자\n| 연산자 | 의미 | 예시 | 결과 |\n|--------|------|------|------|\n| + | 더하기 | 7 + 3 | 10 |\n| - | 빼기 | 7 - 3 | 4 |\n| * | 곱하기 | 7 * 3 | 21 |\n| / | 나누기 | 7 / 3 | 2.333... |\n| // | 몫 | 7 // 3 | 2 |\n| % | 나머지 | 7 % 3 | 1 |\n| ** | 거듭제곱 | 2 ** 10 | 1024 |\n\n## 연산 우선순위\n\`\`\`python\n# 1순위: 괄호 ()\n# 2순위: 거듭제곱 **\n# 3순위: 곱하기, 나누기 *, /, //, %\n# 4순위: 더하기, 빼기 +, -\n\nresult1 = 2 + 3 * 4      # 14 (3*4=12, 2+12=14)\nresult2 = (2 + 3) * 4    # 20\nresult3 = 2 ** 3 ** 2    # 512 (3**2=9, 2**9=512)\n\nprint(f"2 + 3 * 4 = {result1}")\nprint(f"(2 + 3) * 4 = {result2}")\nprint(f"2 ** 3 ** 2 = {result3}")\n\`\`\`\n\n## 복합 대입 연산자\n\`\`\`python\nscore = 100\nscore += 10    # score = score + 10 → 110\nscore -= 5     # score = score - 5 → 105\nscore *= 2     # score = score * 2 → 210\nscore //= 3    # score = score // 3 → 70\nprint(f"최종 점수: {score}")\n\`\`\`` },
        { order: 2, title: "산술 연산 실습", type: "practice", content: `# 🧮 산술 연산 실습\n\n## 📌 실습 1: 시간 변환기\n\`\`\`python\ntotal_minutes = 256\nhours = total_minutes // 60\nminutes = total_minutes % 60\nprint(f"{total_minutes}분 = {hours}시간 {minutes}분")\n\`\`\`\n\n## 📌 실습 2: 온도 변환기\n\`\`\`python\ncelsius = 36.5\nfahrenheit = celsius * 9/5 + 32\nprint(f"섭씨 {celsius}°C = 화씨 {fahrenheit:.1f}°F")\n\`\`\`\n\n## 📌 실습 3: 거스름돈 계산기\n\`\`\`python\nprice = 3750\npaid = 10000\nchange = paid - price\n\nc_1000 = change // 1000\nremain = change % 1000\nc_500 = remain // 500\nremain = remain % 500\nc_100 = remain // 100\nremain = remain % 100\n\nprint(f"거스름돈: {change}원")\nprint(f"1000원: {c_1000}장")\nprint(f"500원: {c_500}개")\nprint(f"100원: {c_100}개")\nprint(f"나머지: {remain}원")\n\`\`\`\n\n## 🎯 도전 과제\n1. 초(seconds)를 시, 분, 초로 변환하는 프로그램을 만드세요 (예: 3725초)\n2. 직사각형의 가로/세로를 변수에 넣고 넓이와 둘레를 계산하세요\n3. 평균 점수 계산기를 만들어 보세요 (국어, 영어, 수학 3과목)` },
        { order: 3, title: "비교 연산자", type: "lesson", content: `# ⚖️ 비교 연산자\n\n두 값을 비교하여 **True** 또는 **False**를 반환합니다.\n\n## 비교 연산자 종류\n| 연산자 | 의미 | 예시 | 결과 |\n|--------|------|------|------|\n| == | 같다 | 5 == 5 | True |\n| != | 다르다 | 5 != 3 | True |\n| > | 크다 | 5 > 3 | True |\n| < | 작다 | 5 < 3 | False |\n| >= | 크거나 같다 | 5 >= 5 | True |\n| <= | 작거나 같다 | 3 <= 5 | True |\n\n## 다양한 비교 예시\n\`\`\`python\nage = 15\nprint(f"age == 15: {age == 15}")   # True\nprint(f"age != 20: {age != 20}")   # True\nprint(f"age > 18: {age > 18}")     # False\nprint(f"age >= 13: {age >= 13}")   # True\n\n# 문자열 비교도 가능!\nprint("apple" == "apple")    # True\nprint("apple" == "Apple")    # False (대소문자 구분!)\nprint("a" < "b")             # True (알파벳 순서)\n\`\`\`\n\n## 📌 주의! = 과 == 의 차이\n\`\`\`python\nx = 10     # 대입 (값을 저장)\nx == 10    # 비교 (같은지 확인) → True\n\`\`\`\n\n## 연쇄 비교 (파이썬만의 기능!)\n\`\`\`python\nage = 15\nprint(10 <= age <= 20)  # True (10 이상 20 이하인가?)\nscore = 85\nprint(80 <= score < 90)  # True (80 이상 90 미만인가?)\n\`\`\`` },
        { order: 4, title: "논리 연산자 (and, or, not)", type: "lesson", content: `# 🔗 논리 연산자 (and, or, not)\n\n여러 조건을 **조합**할 때 사용합니다.\n\n## and (그리고)\n두 조건이 **모두 True**일 때만 True\n\`\`\`python\nage = 15\nis_student = True\n\nprint(age >= 13 and age <= 18)        # True\nprint(age >= 13 and is_student)        # True\nprint(age >= 18 and is_student)        # False\n\`\`\`\n\n## or (또는)\n두 조건 중 **하나라도 True**이면 True\n\`\`\`python\nday = "토요일"\nprint(day == "토요일" or day == "일요일")  # True (주말인가?)\nprint(day == "월요일" or day == "화요일")  # False\n\`\`\`\n\n## not (아닌)\n조건을 **반대로** 바꿈\n\`\`\`python\nis_raining = False\nprint(not is_raining)  # True\n\nage = 15\nprint(not age >= 18)   # True (성인이 아닌가?)\n\`\`\`\n\n## 📌 진리표\n| A | B | A and B | A or B | not A |\n|---|---|---------|--------|-------|\n| True | True | True | True | False |\n| True | False | False | True | False |\n| False | True | False | True | True |\n| False | False | False | False | True |\n\n## 📌 복합 조건 예시\n\`\`\`python\nscore = 85\nattendance = 90\n\n# 점수 80 이상 "그리고" 출석률 80% 이상이면 합격\npassed = score >= 80 and attendance >= 80\nprint(f"합격 여부: {passed}")  # True\n\n# 점수 95 이상 "또는" 출석률 100%이면 우수상\nexcellent = score >= 95 or attendance == 100\nprint(f"우수상: {excellent}")  # False\n\`\`\`` },
        { order: 5, title: "비교·논리 연산 실습", type: "practice", content: `# ✏️ 비교·논리 연산 실습\n\n## 📌 실습 1: 자격 조건 확인기\n\`\`\`python\nage = 16\nhas_id = True\nparental_consent = True\n\ncan_register = age >= 14 and has_id\ncan_drive = age >= 18\ncan_vote = age >= 18\ncan_work = age >= 15 or (age >= 13 and parental_consent)\n\nprint("📋 자격 조건 확인")\nprint(f"  회원가입 가능: {can_register}")\nprint(f"  운전 가능: {can_drive}")\nprint(f"  투표 가능: {can_vote}")\nprint(f"  아르바이트 가능: {can_work}")\n\`\`\`\n\n## 📌 실습 2: 윤년 판별기\n\`\`\`python\nyear = 2024\nis_leap = (year % 4 == 0 and year % 100 != 0) or (year % 400 == 0)\nprint(f"{year}년은 윤년인가? {is_leap}")\n\n# 여러 해 테스트\nfor y in [2000, 1900, 2024, 2023, 2100]:\n    leap = (y % 4 == 0 and y % 100 != 0) or (y % 400 == 0)\n    print(f"  {y}년: {'윤년' if leap else '평년'}")\n\`\`\`\n\n## 🎯 도전 과제\n1. 점수(0~100)가 주어졌을 때 합격(60점 이상) 여부를 판별하세요\n2. 나이가 13~19세 사이인지 확인하는 코드를 작성하세요\n3. 세 과목 점수가 모두 80점 이상인지 확인하세요` },
        { order: 6, title: "문자열 연산자와 메서드", type: "lesson", content: `# 🔤 문자열 연산자와 메서드\n\n## in 연산자\n문자열 안에 특정 문자가 포함되어 있는지 확인합니다.\n\`\`\`python\nmessage = "파이썬은 재미있어요"\nprint("파이썬" in message)     # True\nprint("자바" in message)       # False\nprint("파이썬" not in message) # False\n\`\`\`\n\n## 문자열 메서드\n\`\`\`python\ntext = "Hello, Python World!"\n\n# 대소문자 변환\nprint(text.upper())        # HELLO, PYTHON WORLD!\nprint(text.lower())        # hello, python world!\nprint(text.title())        # Hello, Python World!\n\n# 공백 제거\nspaced = "  안녕하세요  "\nprint(spaced.strip())      # "안녕하세요" (양쪽 공백 제거)\n\n# 치환\nprint(text.replace("Python", "파이썬"))  # Hello, 파이썬 World!\n\n# 분리\ncsv = "사과,바나나,포도,딸기"\nfruits = csv.split(",")\nprint(fruits)  # ['사과', '바나나', '포도', '딸기']\n\n# 결합\nprint(" | ".join(fruits))  # 사과 | 바나나 | 포도 | 딸기\n\n# 검색\nprint(text.find("Python"))    # 7 (위치)\nprint(text.count("l"))        # 3 (개수)\nprint(text.startswith("Hello"))  # True\nprint(text.endswith("!"))        # True\n\`\`\`` },
        { order: 7, title: "문자열 슬라이싱", type: "lesson", content: `# ✂️ 문자열 슬라이싱\n\n문자열의 일부분을 잘라내는 것을 **슬라이싱(slicing)**이라고 합니다.\n\n## 기본 문법: [시작:끝]\n\`\`\`python\ntext = "Hello, Python!"\n\nprint(text[0:5])    # Hello (0~4까지)\nprint(text[7:13])   # Python (7~12까지)\nprint(text[:5])     # Hello (처음부터 5 전까지)\nprint(text[7:])     # Python! (7부터 끝까지)\nprint(text[:])      # Hello, Python! (전체 복사)\n\`\`\`\n\n## 스텝 지정: [시작:끝:스텝]\n\`\`\`python\nnumbers = "0123456789"\n\nprint(numbers[::2])    # 02468 (2칸씩 건너뛰기)\nprint(numbers[1::2])   # 13579 (홀수 인덱스)\nprint(numbers[::-1])   # 9876543210 (뒤집기!)\nprint(numbers[8:2:-1]) # 876543 (8부터 3까지 역순)\n\`\`\`\n\n## 활용 예시\n\`\`\`python\nphone = "010-1234-5678"\nprint(f"앞자리: {phone[:3]}")      # 010\nprint(f"뒷자리: {phone[-4:]}")     # 5678\nprint(f"숨김: {phone[:4]}****{phone[-4:]}")  # 010-****5678\n\nemail = "student@school.kr"\nat_pos = email.find("@")\nprint(f"아이디: {email[:at_pos]}")      # student\nprint(f"도메인: {email[at_pos+1:]}")    # school.kr\n\n# 팰린드롬(회문) 확인\nword = "level"\nprint(f"'{word}'은(는) 회문인가? {word == word[::-1]}")  # True\n\`\`\`\n\n## 🎯 도전 과제\n1. 주민번호 "010101-3456789"에서 성별 코드(7번째 숫자)를 추출하세요\n2. 문자열을 뒤집어 출력하세요\n3. 전화번호의 가운데 4자리를 ****로 바꿔 출력하세요` },
        { order: 8, title: "연산자 종합 실습", type: "practice", content: `# 🎯 연산자 종합 실습\n\n## 📌 실습 1: 성적 평가 프로그램\n\`\`\`python\nkor = 85\neng = 92\nmath = 78\n\ntotal = kor + eng + math\navg = total / 3\n\nprint("📊 성적표")\nprint("=" * 25)\nprint(f"  국어: {kor}점")\nprint(f"  영어: {eng}점")\nprint(f"  수학: {math}점")\nprint("-" * 25)\nprint(f"  합계: {total}점")\nprint(f"  평균: {avg:.1f}점")\nprint("-" * 25)\nprint(f"  합격: {'합격 ✅' if avg >= 60 else '불합격 ❌'}")\nprint(f"  전과목 80점 이상: {kor >= 80 and eng >= 80 and math >= 80}")\n\`\`\`\n\n## 📌 실습 2: 단위 변환기\n\`\`\`python\nkm = 42.195  # 마라톤 거리\n\nmiles = km * 0.621371\nmeters = km * 1000\ncm = km * 100000\n\nprint(f"🏃 마라톤 거리 변환")\nprint(f"  {km} km")\nprint(f"  = {miles:.2f} miles")\nprint(f"  = {meters:,.0f} m")\nprint(f"  = {cm:,.0f} cm")\n\`\`\`\n\n## 📌 실습 3: 비밀 메시지 만들기\n\`\`\`python\nmessage = "나는 파이썬을 좋아합니다"\n\n# 짝수 인덱스 글자만 추출\nhidden = message[::2]\nprint(f"숨겨진 메시지: {hidden}")\n\n# 뒤집기\nreversed_msg = message[::-1]\nprint(f"뒤집은 메시지: {reversed_msg}")\n\n# 글자 바꾸기\nnew_msg = message.replace("파이썬", "코딩")\nprint(f"변환된 메시지: {new_msg}")\n\`\`\`\n\n## 🎯 도전 과제\n1. 쇼핑 목록의 총 금액과 평균 가격을 계산하세요\n2. 주어진 문자열에서 모음(a,e,i,o,u)이 몇 개인지 세어보세요\n3. 간단한 암호화/복호화 프로그램을 만들어 보세요` },
    ],
    quizzes: [
        { question: "17 // 5의 결과는?", options: JSON.stringify(["3.4", "3", "2", "4"]), answer: 1, order: 1 },
        { question: "not (5 > 3) 의 결과는?", options: JSON.stringify(["True", "False", "에러", "None"]), answer: 1, order: 2 },
        { question: "'Hello'[::-1]의 결과는?", options: JSON.stringify(["Hello", "olleH", "o", "H"]), answer: 1, order: 3 },
    ],
};

export const session4: SessionData = {
    number: 4,
    title: "조건문 (if문)",
    description: "if, elif, else를 사용해 프로그램에 판단 능력을 부여합니다.",
    lessons: [
        { order: 1, title: "if문 기본 구조", type: "lesson", content: `# 🔀 if문 기본 구조\n\n조건문은 프로그램이 **상황에 따라 다른 행동**을 하게 합니다.\n\n## 기본 if문\n\`\`\`python\nage = 15\n\nif age >= 18:\n    print("성인입니다")\n    print("투표할 수 있습니다")\n\nprint("프로그램 끝")  # 이건 항상 실행\n\`\`\`\n\n## 📌 핵심 포인트\n1. **조건식 뒤에 콜론(:)** 필수!\n2. 조건이 True일 때 실행할 코드는 **들여쓰기(4칸 공백)** 필수!\n3. 들여쓰기를 하지 않은 코드는 if문과 상관없이 항상 실행\n\n## if-else문\n\`\`\`python\nage = 15\n\nif age >= 18:\n    print("성인입니다")\nelse:\n    print("미성년입니다")\n\`\`\`\n\n## if-elif-else문\n\`\`\`python\nscore = 85\n\nif score >= 90:\n    grade = "A"\nelif score >= 80:\n    grade = "B"\nelif score >= 70:\n    grade = "C"\nelif score >= 60:\n    grade = "D"\nelse:\n    grade = "F"\n\nprint(f"점수: {score}, 등급: {grade}")  # 점수: 85, 등급: B\n\`\`\`\n\n> 📌 elif는 "else if"의 줄임말로, 여러 조건을 순서대로 확인합니다.` },
        { order: 2, title: "if문 실습 - 짝홀수 판별", type: "practice", content: `# ✏️ if문 실습 - 짝홀수 판별\n\n## 📌 실습 1: 짝수/홀수 판별\n\`\`\`python\nnumber = 17\n\nif number % 2 == 0:\n    print(f"{number}은(는) 짝수입니다")\nelse:\n    print(f"{number}은(는) 홀수입니다")\n\`\`\`\n\n## 📌 실습 2: 양수/음수/영 판별\n\`\`\`python\nnum = -5\n\nif num > 0:\n    print(f"{num}은(는) 양수입니다 ✅")\nelif num < 0:\n    print(f"{num}은(는) 음수입니다 ❌")\nelse:\n    print(f"{num}은(는) 영(0)입니다 ⭕")\n\`\`\`\n\n## 📌 실습 3: 계절 판별기\n\`\`\`python\nmonth = 8\n\nif month in [3, 4, 5]:\n    season = "🌸 봄"\nelif month in [6, 7, 8]:\n    season = "☀️ 여름"\nelif month in [9, 10, 11]:\n    season = "🍂 가을"\nelif month in [12, 1, 2]:\n    season = "❄️ 겨울"\nelse:\n    season = "❓ 알 수 없음"\n\nprint(f"{month}월은 {season}입니다")\n\`\`\`\n\n## 🎯 도전 과제\n1. 입력된 숫자가 3의 배수인지, 5의 배수인지, 둘 다인지 판별하세요\n2. 나이에 따라 어린이(~12), 청소년(13~18), 성인(19~) 을 구분하세요\n3. 점수에 따라 A+, A, B+, B, C+, C, D, F 등급을 매기세요` },
        { order: 3, title: "중첩 if문과 조건 조합", type: "lesson", content: `# 🔗 중첩 if문과 조건 조합\n\n## 중첩 if문\nif문 안에 또 if문을 넣을 수 있습니다.\n\`\`\`python\nage = 16\nhas_ticket = True\n\nif age >= 12:\n    print("나이 조건 통과 ✅")\n    if has_ticket:\n        print("티켓 확인 완료 ✅")\n        print("🎢 놀이기구 탑승 가능!")\n    else:\n        print("티켓이 필요합니다 ❌")\nelse:\n    print("12세 이상만 탑승 가능합니다 ❌")\n\`\`\`\n\n## 논리 연산자로 조건 조합\n중첩 if문 대신 and, or를 활용할 수 있습니다.\n\`\`\`python\n# 중첩 if문 대신\nif age >= 12 and has_ticket:\n    print("🎢 놀이기구 탑승 가능!")\nelse:\n    print("탑승 불가 ❌")\n\`\`\`\n\n## 복잡한 조건 예시\n\`\`\`python\nscore = 85\nattendance = 92\nreport = True\n\n# 조건: 점수 70+ AND (출석 80%+ OR 보고서 제출)\nif score >= 70 and (attendance >= 80 or report):\n    print("합격입니다! 🎉")\nelse:\n    print("불합격입니다 😢")\n\`\`\`\n\n## 조건부 표현식 (삼항 연산자)\n간단한 if-else를 한 줄로 작성할 수 있습니다.\n\`\`\`python\nage = 15\nstatus = "성인" if age >= 18 else "미성년"\nprint(status)  # 미성년\n\nnumber = 7\nresult = "짝수" if number % 2 == 0 else "홀수"\nprint(f"{number}은 {result}")  # 7은 홀수\n\`\`\`` },
        { order: 4, title: "실전 if문 프로젝트", type: "practice", content: `# 🎮 실전 if문 프로젝트\n\n## 📌 실습 1: BMI 계산 및 판정\n\`\`\`python\nweight = 65  # kg\nheight = 170  # cm\n\nbmi = weight / (height / 100) ** 2\n\nprint(f"📊 BMI 계산기")\nprint(f"  체중: {weight}kg")\nprint(f"  키: {height}cm")\nprint(f"  BMI: {bmi:.1f}")\nprint()\n\nif bmi < 18.5:\n    result = "저체중 🔵"\nelif bmi < 23:\n    result = "정상 🟢"\nelif bmi < 25:\n    result = "과체중 🟡"\nelif bmi < 30:\n    result = "비만 🟠"\nelse:\n    result = "고도비만 🔴"\n\nprint(f"  판정: {result}")\n\`\`\`\n\n## 📌 실습 2: 교통요금 계산기\n\`\`\`python\nage = 15\ndistance = 12  # km\n\n# 기본 요금 결정\nif age <= 5:\n    base_fare = 0\n    category = "무료 (영유아)"\nelif age <= 12:\n    base_fare = 450\n    category = "어린이"\nelif age <= 18:\n    base_fare = 720\n    category = "청소년"\nelif age >= 65:\n    base_fare = 0\n    category = "무료 (경로)"\nelse:\n    base_fare = 1250\n    category = "성인"\n\n# 거리 추가 요금\nextra = max(0, (distance - 10)) * 100\ntotal = base_fare + extra\n\nprint("🚌 교통요금 계산")\nprint(f"  구분: {category}")\nprint(f"  거리: {distance}km")\nprint(f"  기본요금: {base_fare:,}원")\nprint(f"  추가요금: {extra:,}원")\nprint(f"  총 요금: {total:,}원")\n\`\`\`\n\n## 📌 실습 3: 가위바위보 게임\n\`\`\`python\nimport random\n\nchoices = ["가위", "바위", "보"]\ncomputer = random.choice(choices)\nplayer = "바위"  # 플레이어 선택\n\nprint(f"🎮 가위바위보!")\nprint(f"  나: {player}")\nprint(f"  컴퓨터: {computer}")\n\nif player == computer:\n    print("  결과: 무승부! 🤝")\nelif (player == "가위" and computer == "보") or \\\n     (player == "바위" and computer == "가위") or \\\n     (player == "보" and computer == "바위"):\n    print("  결과: 승리! 🎉")\nelse:\n    print("  결과: 패배! 😢")\n\`\`\`` },
        { order: 5, title: "들여쓰기의 중요성", type: "lesson", content: `# 📐 들여쓰기의 중요성\n\n파이썬에서 **들여쓰기(indentation)**는 문법의 핵심입니다! 다른 언어와 달리 중괄호 {} 대신 들여쓰기로 코드 블록을 구분합니다.\n\n## 올바른 들여쓰기\n\`\`\`python\nif True:\n    print("이것은")      # 4칸 들여쓰기\n    print("같은 블록")    # 4칸 들여쓰기\nprint("if문 바깥")       # 들여쓰기 없음\n\`\`\`\n\n## 잘못된 들여쓰기 예시\n\`\`\`python\n# IndentationError!\nif True:\nprint("에러!")  # 들여쓰기 없음\n\n# IndentationError!\nif True:\n    print("첫 번째")\n      print("두 번째")  # 들여쓰기 불일치\n\`\`\`\n\n## 중첩 들여쓰기\n\`\`\`python\nif True:\n    print("1단계")      # 4칸\n    if True:\n        print("2단계")  # 8칸\n        if True:\n            print("3단계")  # 12칸\n\`\`\`\n\n## 💡 팁\n- **항상 4칸 공백**을 사용하세요 (Tab 키가 자동으로 처리)\n- **같은 블록은 같은 들여쓰기** 레벨이어야 합니다\n- 탭과 공백을 섞지 마세요!\n\n## 🎯 실습\n다음 코드의 들여쓰기를 수정해 보세요:\n\`\`\`python\nscore = 85\nif score >= 90:\nprint("A학점")\nelif score >= 80:\nprint("B학점")\nelse:\nprint("C학점 이하")\n\`\`\`` },
        { order: 6, title: "실전 프로젝트 - 미니 퀴즈 게임", type: "practice", content: `# 🎯 실전 프로젝트 - 미니 퀴즈 게임\n\n조건문을 활용해서 간단한 퀴즈 게임을 만들어 봅시다!\n\n## 예시 코드\n\`\`\`python\nimport random\n\nprint("🧠 파이썬 퀴즈 게임!")\nprint("=" * 30)\n\nscore = 0\ntotal = 3\n\n# 퀴즈 1\nprint("\\nQ1. print()는 어떤 기능을 하나요?")\nprint("  1) 입력받기  2) 출력하기  3) 계산하기  4) 저장하기")\nanswer1 = 2\nif answer1 == 2:\n    print("  ✅ 정답!")\n    score += 1\nelse:\n    print("  ❌ 오답! 정답: 2) 출력하기")\n\n# 퀴즈 2\nprint("\\nQ2. 5 // 2의 결과는?")\nprint("  1) 2.5  2) 2  3) 3  4) 1")\nanswer2 = 2\nif answer2 == 2:\n    print("  ✅ 정답!")\n    score += 1\nelse:\n    print("  ❌ 오답! 정답: 2) 2")\n\n# 퀴즈 3\nprint("\\nQ3. 파이썬의 주석 기호는?")\nprint("  1) //  2) /*  3) #  4) --")\nanswer3 = 3\nif answer3 == 3:\n    print("  ✅ 정답!")\n    score += 1\nelse:\n    print("  ❌ 오답! 정답: 3) #")\n\n# 결과\nprint("\\n" + "=" * 30)\nprint(f"📊 결과: {score}/{total} ({score/total*100:.0f}점)")\n\nif score == total:\n    print("🏆 만점! 대단합니다!")\nelif score >= total * 0.6:\n    print("👍 잘했어요!")\nelse:\n    print("📚 조금 더 공부해 볼까요?")\n\`\`\`\n\n## 🎯 도전 과제\n1. 퀴즈 문제를 5개 이상으로 늘려보세요\n2. 난이도(쉬움/보통/어려움)에 따라 문제를 다르게 출력하세요\n3. 랜덤으로 문제 순서를 바꿔보세요\n4. 각 문제에 힌트 기능을 추가해 보세요` },
        { order: 7, title: "match-case문 (파이썬 3.10+)", type: "lesson", content: `# 🎛️ match-case문\n\n파이썬 3.10부터 추가된 **패턴 매칭** 문법입니다. 여러 조건을 깔끔하게 분류할 때 유용합니다.\n\n## 기본 구조\n\`\`\`python\ncommand = "시작"\n\nmatch command:\n    case "시작":\n        print("🟢 게임을 시작합니다")\n    case "일시정지":\n        print("⏸️ 일시정지합니다")\n    case "종료":\n        print("🔴 게임을 종료합니다")\n    case _:\n        print("❓ 알 수 없는 명령입니다")\n\`\`\`\n\n## if-elif vs match-case 비교\n\`\`\`python\n# if-elif 버전\nday_num = 3\nif day_num == 1:\n    day = "월요일"\nelif day_num == 2:\n    day = "화요일"\nelif day_num == 3:\n    day = "수요일"\nelse:\n    day = "알 수 없음"\n\n# match-case 버전 (더 깔끔!)\nmatch day_num:\n    case 1: day = "월요일"\n    case 2: day = "화요일"\n    case 3: day = "수요일"\n    case _: day = "알 수 없음"\n\nprint(f"{day_num} → {day}")\n\`\`\`\n\n## 여러 패턴 매칭\n\`\`\`python\nstatus_code = 404\n\nmatch status_code:\n    case 200:\n        print("✅ 성공")\n    case 301 | 302:\n        print("↗️ 리다이렉트")\n    case 404:\n        print("❌ 페이지를 찾을 수 없음")\n    case 500 | 502 | 503:\n        print("🔥 서버 에러")\n    case _:\n        print("❓ 알 수 없는 상태")\n\`\`\`` },
        { order: 8, title: "종합 프로젝트 - 텍스트 어드벤처", type: "practice", content: `# 🏰 종합 프로젝트 - 텍스트 어드벤처\n\n조건문을 활용한 간단한 텍스트 어드벤처 게임입니다!\n\n\`\`\`python\nimport random\n\nprint("🏰 던전 어드벤처 🏰")\nprint("=" * 35)\nprint("당신은 용감한 모험가입니다.")\nprint("던전 입구에 서 있습니다.\\n")\n\n# 캐릭터 설정\nhp = 100\natk = 15\ngold = 0\n\n# 1단계: 입구\nprint("🚪 앞에 두 개의 문이 있습니다.")\nchoice1 = "왼쪽"  # "왼쪽" 또는 "오른쪽"\n\nif choice1 == "왼쪽":\n    print("\\n⬅️ 왼쪽 문을 열었습니다!")\n    print("💰 보물 상자를 발견했습니다!")\n    gold += 50\n    print(f"  금화 50개 획득! (보유: {gold})")\nelse:\n    print("\\n➡️ 오른쪽 문을 열었습니다!")\n    print("👹 고블린이 나타났습니다!")\n    damage = random.randint(5, 20)\n    hp -= damage\n    print(f"  {damage} 데미지를 받았습니다! (HP: {hp})")\n\n# 2단계: 전투\nprint("\\n⚔️ 해골 전사가 길을 막습니다!")\nprint("  1: 전투  2: 도주  3: 협상")\nchoice2 = 1\n\nif choice2 == 1:\n    print("\\n🗡️ 전투를 시작합니다!")\n    monster_hp = 30\n    while monster_hp > 0 and hp > 0:\n        damage = random.randint(10, atk)\n        monster_hp -= damage\n        print(f"  당신의 공격! {damage} 데미지 (적 HP: {max(0, monster_hp)})")\n        if monster_hp > 0:\n            m_damage = random.randint(5, 15)\n            hp -= m_damage\n            print(f"  적의 반격! {m_damage} 데미지 (내 HP: {max(0, hp)})")\n    \n    if hp > 0:\n        print("  🎉 승리! 경험치와 금화 획득!")\n        gold += 30\n    else:\n        print("  💀 패배...")\n        \nelif choice2 == 2:\n    print("\\n🏃 도주합니다!")\n    if random.random() > 0.5:\n        print("  도주 성공!")\n    else:\n        hp -= 10\n        print(f"  도주 실패... 10 데미지! (HP: {hp})")\nelse:\n    print("\\n🤝 협상을 시도합니다...")\n    if gold >= 30:\n        gold -= 30\n        print("  금화 30개를 주고 통과했습니다!")\n    else:\n        print("  금화가 부족합니다! 전투 시작!")\n        hp -= 15\n\n# 결과\nprint("\\n" + "=" * 35)\nprint("📊 모험 결과")\nprint(f"  ❤️ HP: {max(0, hp)}/100")\nprint(f"  💰 금화: {gold}개")\n\nif hp > 0:\n    print("  🏆 살아서 던전을 탈출했습니다!")\nelse:\n    print("  💀 던전에서 쓰러졌습니다...")\n\`\`\`\n\n## 🎯 도전 과제\n1. 3단계 이상의 스토리를 추가해 보세요\n2. 아이템 시스템을 만들어 보세요 (물약, 무기 등)\n3. 다양한 몬스터를 추가해 보세요` },
    ],
    quizzes: [
        { question: "if문에서 조건식 뒤에 반드시 써야 하는 기호는?", options: JSON.stringify([";", ":", "{", "("]), answer: 1, order: 1 },
        { question: "elif는 무엇의 줄임말인가요?", options: JSON.stringify(["else finally", "else if", "element if", "else info"]), answer: 1, order: 2 },
        { question: "파이썬에서 코드 블록을 구분하는 방법은?", options: JSON.stringify(["중괄호 {}", "들여쓰기", "세미콜론 ;", "괄호 ()"]), answer: 1, order: 3 },
    ],
};
