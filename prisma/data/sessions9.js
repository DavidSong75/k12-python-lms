"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.session9 = void 0;
exports.session9 = {
    number: 9, title: "문자열 다루기", description: "문자열의 다양한 메서드와 포맷팅을 배웁니다.",
    lessons: [
        { order: 1, title: "문자열 메서드 총정리", type: "lesson", content: `# 🔤 문자열 메서드 총정리\n\n## 검색 메서드\n\`\`\`python\ntext = "Hello, Python World! Python is great."\n\nprint(text.find("Python"))      # 7 (첫 번째 위치)\nprint(text.rfind("Python"))     # 21 (마지막 위치)\nprint(text.count("Python"))     # 2 (등장 횟수)\nprint(text.startswith("Hello")) # True\nprint(text.endswith("great."))  # True\nprint("Python" in text)         # True\n\`\`\`\n\n## 변환 메서드\n\`\`\`python\ntext = "hello, WORLD"\nprint(text.upper())       # HELLO, WORLD\nprint(text.lower())       # hello, world\nprint(text.title())       # Hello, World\nprint(text.capitalize())  # Hello, world\nprint(text.swapcase())    # HELLO, world\n\`\`\`\n\n## 공백 및 정렬\n\`\`\`python\ntext = "  hello world  "\nprint(text.strip())    # "hello world"\nprint(text.lstrip())   # "hello world  "\nprint(text.rstrip())   # "  hello world"\n\nword = "Python"\nprint(word.center(20, "="))  # =======Python=======\nprint(word.ljust(20, "-"))   # Python--------------\nprint(word.rjust(20, "-"))   # --------------Python\n\`\`\`\n\n## 판별 메서드\n\`\`\`python\nprint("123".isdigit())      # True\nprint("abc".isalpha())      # True\nprint("abc123".isalnum())   # True\nprint("   ".isspace())      # True\nprint("Hello".isupper())    # False\n\`\`\`` },
        { order: 2, title: "문자열 포맷팅 심화", type: "lesson", content: `# ✨ 문자열 포맷팅 심화\n\n## f-string 고급 기법\n\`\`\`python\n# 정렬과 폭 지정\nfor item, price in [("커피", 4500), ("케이크", 6000), ("쿠키", 2500)]:\n    print(f"{item:<8}{price:>8,}원")\n\n# 숫자 포맷\npi = 3.141592653589\nprint(f"기본: {pi}")           # 3.141592653589\nprint(f"소수 2자리: {pi:.2f}")  # 3.14\nprint(f"백분율: {0.856:.1%}")   # 85.6%\nprint(f"이진수: {42:b}")        # 101010\nprint(f"8진수: {42:o}")         # 52\nprint(f"16진수: {42:x}")        # 2a\nprint(f"천 단위: {1234567:,}")   # 1,234,567\n\`\`\`\n\n## format() 메서드\n\`\`\`python\nprint("{} + {} = {}".format(3, 5, 8))\nprint("{name}은 {age}세".format(name="홍길동", age=15))\nprint("{0}는 {1}를 {0}".format("고양이", "좋아합니다"))\n\`\`\`\n\n## 실전 활용: 표 만들기\n\`\`\`python\ndata = [\n    ("홍길동", 85, 92, 78),\n    ("김철수", 90, 88, 95),\n    ("이영희", 95, 82, 89),\n]\n\nprint(f"{'이름':^8}{'국어':^6}{'영어':^6}{'수학':^6}{'평균':^8}")\nprint("-" * 34)\nfor name, kor, eng, math in data:\n    avg = (kor + eng + math) / 3\n    print(f"{name:^8}{kor:^6}{eng:^6}{math:^6}{avg:^8.1f}")\n\`\`\`` },
        { order: 3, title: "문자열 실습", type: "practice", content: `# ✏️ 문자열 실습\n\n## 📌 실습 1: 이메일 검증기\n\`\`\`python\ndef validate_email(email):\n    checks = []\n    checks.append(("@ 포함", "@" in email))\n    checks.append((". 포함", "." in email))\n    if "@" in email:\n        parts = email.split("@")\n        checks.append(("@ 하나만", email.count("@") == 1))\n        checks.append(("아이디 있음", len(parts[0]) > 0))\n        checks.append(("도메인 있음", "." in parts[1]))\n    \n    print(f"📧 이메일 검증: {email}")\n    all_pass = True\n    for check_name, result in checks:\n        status = "✅" if result else "❌"\n        print(f"  {status} {check_name}")\n        if not result:\n            all_pass = False\n    print(f"  결과: {'유효' if all_pass else '유효하지 않음'}")\n\nvalidate_email("student@school.kr")\nvalidate_email("invalid-email")\n\`\`\`\n\n## 📌 실습 2: 문장 분석기\n\`\`\`python\ndef analyze_text(text):\n    words = text.split()\n    sentences = text.count(".") + text.count("!") + text.count("?")\n    \n    print("📊 문장 분석 결과")\n    print(f"  글자 수: {len(text)}")\n    print(f"  단어 수: {len(words)}")\n    print(f"  문장 수: {sentences}")\n    print(f"  가장 긴 단어: {max(words, key=len)}")\n    \n    vowels = sum(1 for c in text.lower() if c in "aeiou")\n    print(f"  모음 수: {vowels}")\n\nanalyze_text("Python is an amazing programming language. It is easy to learn!")\n\`\`\`\n\n## 🎯 도전 과제\n1. 주민등록번호 마스킹 함수를 만드세요 (뒷자리 *****)\n2. 카멜케이스를 스네이크케이스로 변환하세요 (helloWorld → hello_world)\n3. 팰린드롬 확인기를 만드세요 (공백, 대소문자 무시)` },
        { order: 4, title: "정규표현식 맛보기", type: "lesson", content: "# \uD83D\uDD0D 정규표현식 맛보기\n\n**정규표현식(regex)**은 문자열에서 패턴을 찾는 강력한 도구입니다.\n\n## 기본 사용법\n```python\nimport re\n\ntext = \"제 전화번호는 010-1234-5678이고, 이메일은 student@school.kr입니다.\"\n\n# 전화번호 찾기\nphone = re.findall(r\"\\d{3}-\\d{4}-\\d{4}\", text)\nprint(f\"전화번호: {phone}\")  # ['010-1234-5678']\n\n# 이메일 찾기\nemail = re.findall(r\"\\w+@\\w+\\.\\w+\", text)\nprint(f\"이메일: {email}\")  # ['student@school.kr']\n```\n\n## 주요 패턴\n| 패턴 | 의미 | 예시 |\n|------|------|------|\n| \\\\d | 숫자 | 0-9 |\n| \\\\w | 문자/숫자/_ | a-z, 0-9 |\n| \\\\s | 공백 | 스페이스, 탭 |\n| . | 아무 문자 | 모든 문자 |\n| * | 0회 이상 | ab* -> a, ab, abb |\n| + | 1회 이상 | ab+ -> ab, abb |\n| {n} | n회 정확히 | a{3} -> aaa |\n\n## 활용 예시\n```python\nimport re\n\n# 숫자만 추출\ntext = \"가격은 15,000원이고 할인가는 12,500원입니다\"\nprices = re.findall(r\"[\\d,]+\", text)\nprint(prices)  # ['15,000', '12,500']\n\n# 특정 패턴 치환\nresult = re.sub(r\"\\d{4}\", \"****\", \"카드번호: 1234-5678-9012-3456\")\nprint(result)  # 카드번호: ****-****-****-****\n```" },
        { order: 5, title: "문자열 프로젝트 - 마크다운 파서", type: "practice", content: "# \uD83D\uDCDD 문자열 프로젝트 - 간이 마크다운 파서\n\n문자열 처리를 활용한 간단한 마크다운 변환기입니다.\n\n```python\ndef parse_markdown(md_text):\n    lines = md_text.split('\\n')\n    result = []\n    \n    for line in lines:\n        if line.startswith('### '):\n            result.append(f'[H3] {line[4:]}')\n        elif line.startswith('## '):\n            result.append(f'[H2] {line[3:]}')\n        elif line.startswith('# '):\n            result.append(f'[H1] {line[2:]}')\n        elif line.startswith('- '):\n            result.append(f'  * {line[2:]}')\n        else:\n            result.append(line)\n    \n    return '\\n'.join(result)\n\nmd = '''# 파이썬 학습 노트\n\n## 오늘 배운 것\n- 변수와 자료형\n- 조건문\n- 반복문\n\n### 다음 시간\n함수를 배울 예정'''\n\nprint(parse_markdown(md))\n```\n\n## \uD83C\uDFAF 도전 과제\n1. 코드 블록 인식을 추가하세요\n2. 링크 처리를 추가하세요\n3. 번호 목록(1. 2. 3.) 처리를 추가하세요" },
        { order: 6, title: "문자열 인코딩과 유니코드", type: "lesson", content: `# 🌍 문자열 인코딩과 유니코드\n\n## ASCII와 유니코드\n\`\`\`python\n# ASCII: 영문 기본 코드 (0~127)\nprint(ord('A'))   # 65\nprint(ord('a'))   # 97\nprint(chr(65))    # A\nprint(chr(97))    # a\n\n# 유니코드: 전 세계 모든 문자\nprint(ord('가'))   # 44032\nprint(chr(44032))  # 가\n\`\`\`\n\n## 한글 유니코드 활용\n\`\`\`python\n# 한글 범위: 가(0xAC00) ~ 힣(0xD7A3)\nfor i in range(0xAC00, 0xAC00 + 14):  # 가~깋\n    print(chr(i), end=" ")\nprint()\n\n# 알파벳 출력\nfor i in range(26):\n    print(chr(65 + i), end=" ")  # A~Z\nprint()\nfor i in range(26):\n    print(chr(97 + i), end=" ")  # a~z\n\`\`\`\n\n## 이모지 활용\n\`\`\`python\n# 이모지도 유니코드!\nemojis = [chr(0x1F600 + i) for i in range(20)]\nprint("".join(emojis))\n\`\`\`` },
        { order: 7, title: "문자열 종합 프로젝트", type: "practice", content: `# 🎯 문자열 종합 프로젝트\n\n## 📌 실습 1: 비밀번호 강도 검사기\n\`\`\`python\ndef check_password_strength(password):\n    score = 0\n    feedback = []\n    \n    if len(password) >= 8:\n        score += 1\n        feedback.append("✅ 8자 이상")\n    else:\n        feedback.append("❌ 8자 미만")\n    \n    if any(c.isupper() for c in password):\n        score += 1\n        feedback.append("✅ 대문자 포함")\n    else:\n        feedback.append("❌ 대문자 없음")\n    \n    if any(c.islower() for c in password):\n        score += 1\n        feedback.append("✅ 소문자 포함")\n    else:\n        feedback.append("❌ 소문자 없음")\n    \n    if any(c.isdigit() for c in password):\n        score += 1\n        feedback.append("✅ 숫자 포함")\n    else:\n        feedback.append("❌ 숫자 없음")\n    \n    strength = ["매우 약함", "약함", "보통", "강함", "매우 강함"][min(score, 4)]\n    \n    print(f"비밀번호 강도: {strength}")\n    for f in feedback:\n        print(f"  {f}")\n\ncheck_password_strength("Python123!")\ncheck_password_strength("abc")\n\`\`\`\n\n## 📌 실습 2: 텍스트 통계 대시보드\n\`\`\`python\ndef text_dashboard(text):\n    words = text.split()\n    chars = [c for c in text if c.isalpha()]\n    \n    print("📊 텍스트 통계 대시보드")\n    print("=" * 35)\n    print(f"  글자 수: {len(text)}")\n    print(f"  단어 수: {len(words)}")\n    print(f"  알파벳: {len(chars)}")\n\ntext_dashboard("Python is great. Python is easy. I love Python programming.")\n\`\`\`\n\n## 🎯 도전 과제\n1. 문자열 압축 함수를 만드세요 (AAABBC → A3B2C1)\n2. ROT13 암호화/복호화를 구현하세요\n3. 단어 빈도 시각화 (막대 그래프)를 만드세요` },
    ],
    quizzes: [
        { question: "'Hello'.upper()의 결과는?", options: JSON.stringify(["hello", "Hello", "HELLO", "hELLO"]), answer: 2, order: 1 },
        { question: "f'{3.14159:.2f}'의 출력은?", options: JSON.stringify(["3.14159", "3.14", "3.1", "3"]), answer: 1, order: 2 },
        { question: "'Python'.find('th')의 결과는?", options: JSON.stringify(["2", "3", "True", "-1"]), answer: 1, order: 3 },
    ],
};
