import { SessionData } from "./sessions1_2";

export const session10: SessionData = {
    number: 10, title: "파일 입출력과 예외처리", description: "파일 읽기/쓰기와 에러를 우아하게 처리하는 방법을 배웁니다.",
    lessons: [
        { order: 1, title: "파일 입출력 개념", type: "lesson", content: `# 📂 파일 입출력 개념\n\n프로그램의 데이터를 파일에 **저장**하고 **읽어오는** 방법을 배웁니다.\n\n> ⚠️ 브라우저 환경에서는 실제 파일 입출력이 제한됩니다. 개념을 이해하는 것에 집중하세요!\n\n## 파일 쓰기\n\`\`\`python\n# 기본 방식\nf = open("hello.txt", "w")\nf.write("안녕하세요!\\n")\nf.write("파이썬 파일 입출력입니다.\\n")\nf.close()  # 반드시 닫아야 함!\n\n# with문 (권장 방식 - 자동으로 닫아줌)\nwith open("hello.txt", "w") as f:\n    f.write("안녕하세요!\\n")\n    f.write("파이썬 파일 입출력입니다.\\n")\n\`\`\`\n\n## 파일 읽기\n\`\`\`python\nwith open("hello.txt", "r") as f:\n    content = f.read()       # 전체 읽기\n    # line = f.readline()    # 한 줄 읽기\n    # lines = f.readlines()  # 모든 줄을 리스트로\nprint(content)\n\`\`\`\n\n## 파일 모드\n| 모드 | 의미 |\n|------|------|\n| "r" | 읽기 (기본) |\n| "w" | 쓰기 (덮어쓰기) |\n| "a" | 추가 (이어쓰기) |\n| "x" | 새 파일 생성 |\n| "b" | 바이너리 모드 |` },
        { order: 2, title: "파일 처리 시뮬레이션 실습", type: "practice", content: `# ✏️ 파일 처리 시뮬레이션\n\n브라우저에서는 실제 파일을 쓸 수 없으므로, 문자열로 시뮬레이션합니다.\n\n## 📌 실습 1: 일기 시스템\n\`\`\`python\n# 파일 대신 리스트로 시뮬레이션\nfile_system = {}\n\ndef write_file(filename, content, mode="w"):\n    if mode == "w":\n        file_system[filename] = content\n    elif mode == "a":\n        file_system[filename] = file_system.get(filename, "") + content\n    print(f"📝 '{filename}' 저장 완료")\n\ndef read_file(filename):\n    if filename in file_system:\n        return file_system[filename]\n    return None\n\n# 일기 쓰기\nwrite_file("diary.txt", "2024-06-01\\n오늘 파이썬을 배웠다. 재미있었다!\\n\\n")\nwrite_file("diary.txt", "2024-06-02\\n반복문을 배웠다. 별찍기가 재미있었다!\\n\\n", "a")\nwrite_file("diary.txt", "2024-06-03\\n함수를 만들었다. 코드가 깔끔해졌다!\\n\\n", "a")\n\ncontent = read_file("diary.txt")\nprint("\\n📖 일기장")\nprint("=" * 30)\nprint(content)\n\`\`\`\n\n## 📌 실습 2: CSV 데이터 처리\n\`\`\`python\ncsv_data = \"\"\"이름,국어,영어,수학\n홍길동,85,92,78\n김철수,90,88,95\n이영희,95,82,89\"\"\"\n\nlines = csv_data.strip().split("\\n")\nheader = lines[0].split(",")\nstudents = []\n\nfor line in lines[1:]:\n    values = line.split(",")\n    student = {\n        header[0]: values[0],\n        header[1]: int(values[1]),\n        header[2]: int(values[2]),\n        header[3]: int(values[3]),\n    }\n    students.append(student)\n\nprint("📊 CSV 데이터 분석")\nfor s in students:\n    avg = (s["국어"] + s["영어"] + s["수학"]) / 3\n    print(f"  {s['이름']}: 평균 {avg:.1f}점")\n\`\`\`\n\n## 🎯 도전 과제\n1. JSON 형식으로 데이터를 저장/로드하는 시뮬레이션을 만드세요\n2. 설정 파일(key=value)을 파싱하는 함수를 만드세요` },
        { order: 3, title: "예외처리 (try-except)", type: "lesson", content: `# 🛡️ 예외처리 (try-except)\n\n프로그램 실행 중 발생하는 에러를 **우아하게 처리**하는 방법입니다.\n\n## 기본 구조\n\`\`\`python\ntry:\n    result = 10 / 0\nexcept ZeroDivisionError:\n    print("0으로 나눌 수 없습니다!")\n\nprint("프로그램 계속 실행~")  # 에러가 나도 여기까지 실행!\n\`\`\`\n\n## 여러 예외 처리\n\`\`\`python\ntry:\n    number = int("abc")\n    result = 10 / number\nexcept ValueError:\n    print("숫자로 변환할 수 없습니다")\nexcept ZeroDivisionError:\n    print("0으로 나눌 수 없습니다")\nexcept Exception as e:\n    print(f"예상치 못한 에러: {e}")\n\`\`\`\n\n## try-except-else-finally\n\`\`\`python\ntry:\n    result = 10 / 2\nexcept ZeroDivisionError:\n    print("에러 발생!")\nelse:\n    print(f"성공: {result}")  # 에러 없을 때만 실행\nfinally:\n    print("항상 실행됩니다")  # 에러 유무 관계없이 항상 실행\n\`\`\`\n\n## 주요 예외 종류\n| 예외 | 발생 상황 |\n|------|----------|\n| ValueError | 잘못된 값 |\n| TypeError | 잘못된 타입 |\n| IndexError | 인덱스 범위 초과 |\n| KeyError | 딕셔너리 키 없음 |\n| ZeroDivisionError | 0으로 나누기 |\n| FileNotFoundError | 파일 없음 |` },
        { order: 4, title: "예외처리 실습", type: "practice", content: `# ✏️ 예외처리 실습\n\n## 📌 실습 1: 안전한 계산기\n\`\`\`python\ndef safe_calculate(a, b, op):\n    try:\n        if op == "+": return a + b\n        elif op == "-": return a - b\n        elif op == "*": return a * b\n        elif op == "/": \n            if b == 0:\n                raise ValueError("0으로 나눌 수 없습니다")\n            return a / b\n        else:\n            raise ValueError(f"지원하지 않는 연산자: {op}")\n    except ValueError as e:\n        return f"에러: {e}"\n    except TypeError:\n        return "에러: 숫자만 입력하세요"\n\nprint(safe_calculate(10, 3, "+"))   # 13\nprint(safe_calculate(10, 0, "/"))   # 에러: 0으로 나눌 수 없음\nprint(safe_calculate(10, 3, "^"))   # 에러: 지원하지 않는 연산자\n\`\`\`\n\n## 📌 실습 2: 데이터 검증\n\`\`\`python\ndef validate_student_data(data):\n    errors = []\n    \n    try:\n        name = data.get("name", "")\n        if not name or len(name) < 2:\n            errors.append("이름은 2자 이상이어야 합니다")\n    except Exception as e:\n        errors.append(f"이름 검증 오류: {e}")\n    \n    try:\n        age = int(data.get("age", 0))\n        if age < 1 or age > 100:\n            errors.append("나이는 1~100 사이여야 합니다")\n    except ValueError:\n        errors.append("나이는 숫자여야 합니다")\n    \n    try:\n        score = float(data.get("score", 0))\n        if score < 0 or score > 100:\n            errors.append("점수는 0~100 사이여야 합니다")\n    except ValueError:\n        errors.append("점수는 숫자여야 합니다")\n    \n    if errors:\n        print("❌ 검증 실패:")\n        for e in errors:\n            print(f"  - {e}")\n    else:\n        print("✅ 검증 성공!")\n\nvalidate_student_data({"name": "홍길동", "age": "15", "score": "85"})\nvalidate_student_data({"name": "", "age": "abc", "score": "150"})\n\`\`\`\n\n## 🎯 도전 과제\n1. 사용자 정의 예외 클래스를 만들어 보세요\n2. 재시도 기능이 있는 함수를 만드세요 (최대 3회 시도)` },
        { order: 5, title: "JSON 데이터 처리", type: "lesson", content: `# 📋 JSON 데이터 처리\n\nJSON은 데이터를 저장하고 교환하는 가장 인기 있는 형식입니다.\n\n## JSON 기본\n\`\`\`python\nimport json\n\n# 파이썬 → JSON 문자열\ndata = {\n    "name": "홍길동",\n    "age": 15,\n    "hobbies": ["코딩", "게임", "독서"],\n    "scores": {"국어": 85, "영어": 92}\n}\n\njson_str = json.dumps(data, ensure_ascii=False, indent=2)\nprint("📋 JSON 출력:")\nprint(json_str)\n\n# JSON 문자열 → 파이썬\nparsed = json.loads(json_str)\nprint(f"\\n이름: {parsed['name']}")\nprint(f"취미: {', '.join(parsed['hobbies'])}")\n\`\`\`\n\n## API 응답 시뮬레이션\n\`\`\`python\nimport json\n\napi_response = '''{\n  "status": "success",\n  "data": {\n    "students": [\n      {"id": 1, "name": "홍길동", "gpa": 3.8},\n      {"id": 2, "name": "김철수", "gpa": 3.5},\n      {"id": 3, "name": "이영희", "gpa": 3.9}\n    ],\n    "total": 3\n  }\n}'''\n\nresult = json.loads(api_response)\n\nif result["status"] == "success":\n    students = result["data"]["students"]\n    print("📊 학생 목록")\n    for s in students:\n        print(f"  {s['id']}. {s['name']} (GPA: {s['gpa']})")\n    print(f"\\n  총 {result['data']['total']}명")\n\`\`\`` },
        { order: 6, title: "모듈과 패키지 소개", type: "lesson", content: `# 📦 모듈과 패키지 소개\n\n## 모듈이란?\n다른 사람이 만든 코드를 **가져다 쓰는** 방법입니다.\n\n## 내장 모듈 활용\n\`\`\`python\n# math 모듈\nimport math\nprint(f"π = {math.pi}")\nprint(f"√16 = {math.sqrt(16)}")\nprint(f"2^10 = {math.pow(2, 10)}")\n\n# random 모듈\nimport random\nprint(f"주사위: {random.randint(1, 6)}")\nprint(f"랜덤: {random.random():.4f}")\nprint(f"선택: {random.choice(['가위','바위','보'])}")\n\n# datetime 모듈\nfrom datetime import datetime, timedelta\nnow = datetime.now()\nprint(f"현재: {now.strftime('%Y-%m-%d %H:%M')}")\nfuture = now + timedelta(days=30)\nprint(f"30일 후: {future.strftime('%Y-%m-%d')}")\n\n# collections 모듈\nfrom collections import Counter\nwords = "파이썬 파이썬 자바 파이썬 자바 C".split()\ncounts = Counter(words)\nprint(f"단어 빈도: {dict(counts)}")\n\`\`\`\n\n## import 방법\n\`\`\`python\nimport math                    # 전체 모듈\nfrom math import sqrt, pi      # 특정 함수만\nfrom math import *              # 모든 것 (비추천)\nimport math as m                # 별명 사용\n\`\`\`` },
        { order: 7, title: "종합 프로젝트 - 데이터 분석기", type: "practice", content: `# 📊 종합 프로젝트 - 데이터 분석기\n\n지금까지 배운 모든 것을 활용한 데이터 분석 프로그램입니다.\n\n\`\`\`python\nimport json\nimport random\nfrom collections import Counter\n\n# 데이터 생성\ndef generate_student_data(count=20):\n    names = ["홍길동","김철수","이영희","박민수","최지은","정도현","강서윤","윤하준","임수빈","한예린",\n             "조민호","송지아","오세준","배서연","류준서","신예진","황도윤","전서현","문지호","고은서"]\n    subjects = ["국어", "영어", "수학", "과학", "사회"]\n    students = []\n    for i in range(min(count, len(names))):\n        scores = {subj: random.randint(50, 100) for subj in subjects}\n        students.append({"name": names[i], "scores": scores})\n    return students\n\ndef analyze(students):\n    subjects = list(students[0]["scores"].keys())\n    \n    print("📊 학생 성적 분석 보고서")\n    print("=" * 50)\n    \n    # 1. 전체 통계\n    all_avgs = []\n    for s in students:\n        avg = sum(s["scores"].values()) / len(s["scores"])\n        all_avgs.append((s["name"], avg))\n    \n    all_avgs.sort(key=lambda x: x[1], reverse=True)\n    \n    print("\\n🏆 전체 석차 (상위 5명)")\n    for rank, (name, avg) in enumerate(all_avgs[:5], 1):\n        medal = "🥇🥈🥉"[rank-1] if rank <= 3 else "  "\n        print(f"  {medal} {rank}위 {name}: {avg:.1f}점")\n    \n    # 2. 과목별 통계\n    print("\\n📈 과목별 통계")\n    for subj in subjects:\n        scores = [s["scores"][subj] for s in students]\n        avg = sum(scores) / len(scores)\n        print(f"  {subj}: 평균 {avg:.1f} | 최고 {max(scores)} | 최저 {min(scores)}")\n    \n    # 3. 등급 분포\n    print("\\n📋 등급 분포")\n    grades = []\n    for _, avg in all_avgs:\n        if avg >= 90: grades.append("A")\n        elif avg >= 80: grades.append("B")\n        elif avg >= 70: grades.append("C")\n        else: grades.append("D")\n    \n    grade_counts = Counter(grades)\n    for grade in ["A", "B", "C", "D"]:\n        count = grade_counts.get(grade, 0)\n        bar = "█" * count\n        print(f"  {grade}: {bar} ({count}명)")\n\nstudents = generate_student_data(15)\nanalyze(students)\n\`\`\`\n\n## 🎯 도전 과제\n1. JSON으로 데이터 저장/로드 기능을 추가하세요\n2. 과목별 히스토그램을 그려보세요\n3. 학생 검색 기능을 추가하세요` },
    ],
    quizzes: [
        { question: "파일을 열 때 자동으로 닫아주는 키워드는?", options: JSON.stringify(["using", "with", "auto", "safe"]), answer: 1, order: 1 },
        { question: "try-except에서 에러 없이 실행될 때 동작하는 블록은?", options: JSON.stringify(["except", "finally", "else", "pass"]), answer: 2, order: 2 },
        { question: "JSON 문자열을 파이썬 객체로 변환하는 함수는?", options: JSON.stringify(["json.dumps()", "json.loads()", "json.parse()", "json.read()"]), answer: 1, order: 3 },
    ],
};

export const session11: SessionData = {
    number: 11, title: "종합 프로젝트", description: "지금까지 배운 모든 내용을 활용해 종합 프로젝트를 완성합니다.",
    lessons: [
        { order: 1, title: "프로젝트 설계 방법", type: "lesson", content: `# 🏗️ 프로젝트 설계 방법\n\n큰 프로그램을 만들 때는 **계획**이 중요합니다!\n\n## 소프트웨어 개발 단계\n1. **요구 분석**: 무엇을 만들 것인가?\n2. **설계**: 어떻게 만들 것인가?\n3. **구현**: 실제 코딩\n4. **테스트**: 제대로 동작하는가?\n5. **개선**: 더 좋게 만들기\n\n## 설계 도구: 의사코드\n실제 코드를 작성하기 전에 **한국어로 로직**을 적어봅니다.\n\n\`\`\`\n프로그램: 가계부\n\n1. 데이터 저장소 준비 (리스트)\n2. 메뉴 보여주기\n   - 1: 수입 추가\n   - 2: 지출 추가\n   - 3: 내역 보기\n   - 4: 통계 보기\n   - 0: 종료\n3. 사용자 선택에 따라 실행\n4. 반복\n\`\`\`\n\n## 프로젝트 구조\n\`\`\`python\n# 좋은 코드 구조\n# 1. 상수 정의\nMAX_ITEMS = 100\n\n# 2. 데이터 구조\ndata = []\n\n# 3. 함수 정의\ndef add_item(item):\n    data.append(item)\n\ndef show_items():\n    for item in data:\n        print(item)\n\n# 4. 메인 로직\ndef main():\n    add_item("테스트")\n    show_items()\n\n# 5. 실행\nmain()\n\`\`\`\n\n## 💡 좋은 코드의 조건\n- 읽기 쉬운 변수명\n- 적절한 주석\n- 함수로 기능 분리\n- 에러 처리` },
        { order: 2, title: "프로젝트 1 - 가계부", type: "practice", content: `# 💰 프로젝트 1 - 가계부\n\n\`\`\`python\nimport json\nfrom datetime import datetime\n\n# 데이터 저장소\nledger = []\n\ndef add_entry(entry_type, amount, category, description):\n    entry = {\n        "date": datetime.now().strftime("%Y-%m-%d"),\n        "type": entry_type,\n        "amount": amount,\n        "category": category,\n        "desc": description\n    }\n    ledger.append(entry)\n    emoji = "💰" if entry_type == "수입" else "💸"\n    print(f"  {emoji} {entry_type}: {amount:,}원 ({category})")\n\ndef show_summary():\n    income = sum(e["amount"] for e in ledger if e["type"] == "수입")\n    expense = sum(e["amount"] for e in ledger if e["type"] == "지출")\n    balance = income - expense\n    \n    print("\\n📊 가계부 요약")\n    print("=" * 35)\n    print(f"  💰 총 수입: {income:>12,}원")\n    print(f"  💸 총 지출: {expense:>12,}원")\n    print(f"  {'─' * 30}")\n    sign = "+" if balance >= 0 else ""\n    print(f"  💵 잔  액: {sign}{balance:>11,}원")\n    \n    # 카테고리별 지출\n    categories = {}\n    for e in ledger:\n        if e["type"] == "지출":\n            cat = e["category"]\n            categories[cat] = categories.get(cat, 0) + e["amount"]\n    \n    if categories:\n        print("\\n  📋 카테고리별 지출")\n        for cat, amount in sorted(categories.items(), key=lambda x: x[1], reverse=True):\n            pct = amount / expense * 100 if expense > 0 else 0\n            bar = "█" * int(pct / 5)\n            print(f"    {cat:<8} {bar} {amount:>8,}원 ({pct:.0f}%)")\n\ndef show_history():\n    print("\\n📋 거래 내역")\n    print(f"{'날짜':<12}{'구분':<6}{'카테고리':<10}{'금액':>10}  {'내용'}")\n    print("-" * 55)\n    for e in ledger:\n        emoji = "💰" if e["type"] == "수입" else "💸"\n        print(f"  {e['date']:<10}{emoji}{e['type']:<4}{e['category']:<10}{e['amount']:>8,}원  {e['desc']}")\n\n# 시뮬레이션 데이터\nadd_entry("수입", 1500000, "급여", "6월 급여")\nadd_entry("지출", 450000, "주거", "월세")\nadd_entry("지출", 300000, "식비", "6월 식비")\nadd_entry("지출", 50000, "교통", "교통카드 충전")\nadd_entry("지출", 80000, "문화", "온라인 강의")\nadd_entry("수입", 200000, "부수입", "과외비")\nadd_entry("지출", 120000, "쇼핑", "의류 구매")\n\nshow_history()\nshow_summary()\n\`\`\`` },
        { order: 3, title: "프로젝트 2 - 퀴즈 앱", type: "practice", content: `# 🧠 프로젝트 2 - 파이썬 퀴즈 앱\n\n\`\`\`python\nimport random\nimport json\n\nquiz_bank = [\n    {"q": "파이썬에서 출력하는 함수는?", "options": ["input()", "print()", "echo()", "show()"], "answer": 1, "category": "기초"},\n    {"q": "리스트에 요소를 추가하는 메서드는?", "options": ["add()", "append()", "insert()", "push()"], "answer": 1, "category": "리스트"},\n    {"q": "딕셔너리의 키를 모두 가져오는 메서드는?", "options": ["items()", "values()", "keys()", "get()"], "answer": 2, "category": "딕셔너리"},\n    {"q": "for문에서 현재 반복을 건너뛰는 키워드는?", "options": ["break", "continue", "pass", "skip"], "answer": 1, "category": "반복문"},\n    {"q": "함수에서 값을 반환하는 키워드는?", "options": ["give", "return", "output", "send"], "answer": 1, "category": "함수"},\n    {"q": "type(3.14)의 결과는?", "options": ["int", "str", "float", "num"], "answer": 2, "category": "자료형"},\n    {"q": "예외를 처리하는 키워드 조합은?", "options": ["if-else", "try-except", "for-in", "while-do"], "answer": 1, "category": "예외처리"},\n    {"q": "'Hello'[::-1]의 결과는?", "options": ["Hello", "olleH", "H", "o"], "answer": 1, "category": "문자열"},\n]\n\ndef run_quiz(num_questions=5):\n    questions = random.sample(quiz_bank, min(num_questions, len(quiz_bank)))\n    score = 0\n    results = []\n    \n    print("🧠 파이썬 퀴즈!")\n    print(f"총 {len(questions)}문제\\n")\n    \n    for i, q in enumerate(questions, 1):\n        print(f"Q{i}. [{q['category']}] {q['q']}")\n        for j, opt in enumerate(q["options"]):\n            print(f"  {j+1}) {opt}")\n        \n        answer = q["answer"]  # 시뮬레이션\n        correct = (answer == q["answer"])\n        if correct:\n            score += 1\n            results.append("✅")\n            print(f"  → 정답! ✅\\n")\n        else:\n            results.append("❌")\n            print(f"  → 오답! 정답: {q['options'][q['answer']]} ❌\\n")\n    \n    # 결과\n    pct = score / len(questions) * 100\n    print("=" * 35)\n    print(f"📊 결과: {score}/{len(questions)} ({pct:.0f}%)")\n    print(f"   {''.join(results)}")\n    \n    if pct == 100: print("🏆 만점! 파이썬 마스터!")\n    elif pct >= 80: print("🌟 우수! 잘 하고 있어요!")\n    elif pct >= 60: print("👍 좋아요! 조금 더 복습해요!")\n    else: print("📚 힘내요! 다시 도전해봐요!")\n\nrun_quiz(5)\n\`\`\`` },
        { order: 4, title: "프로젝트 3 - 텍스트 RPG", type: "practice", content: `# ⚔️ 프로젝트 3 - 텍스트 RPG\n\n\`\`\`python\nimport random\n\nclass Character:\n    def __init__(self, name, hp, atk, defense):\n        self.name = name\n        self.max_hp = hp\n        self.hp = hp\n        self.atk = atk\n        self.defense = defense\n        self.level = 1\n        self.exp = 0\n        self.inventory = []\n    \n    def attack(self, target):\n        damage = max(1, self.atk - target.defense + random.randint(-3, 5))\n        target.hp -= damage\n        return damage\n    \n    def is_alive(self):\n        return self.hp > 0\n    \n    def heal(self, amount):\n        self.hp = min(self.max_hp, self.hp + amount)\n    \n    def status(self):\n        bar_len = 20\n        filled = int(self.hp / self.max_hp * bar_len)\n        bar = "█" * filled + "░" * (bar_len - filled)\n        return f"{self.name} [{bar}] {self.hp}/{self.max_hp} HP"\n\ndef battle(hero, monster):\n    print(f"\\n⚔️ {monster.name} 등장!")\n    turn = 0\n    while hero.is_alive() and monster.is_alive():\n        turn += 1\n        print(f"\\n--- 턴 {turn} ---")\n        print(f"  {hero.status()}")\n        print(f"  {monster.status()}")\n        \n        # 영웅 공격\n        dmg = hero.attack(monster)\n        print(f"  ⚔️ {hero.name}의 공격! {dmg} 데미지!")\n        \n        if not monster.is_alive():\n            exp = random.randint(10, 30)\n            hero.exp += exp\n            print(f"\\n  🎉 {monster.name} 처치! +{exp} 경험치")\n            if hero.exp >= hero.level * 30:\n                hero.level += 1\n                hero.max_hp += 10\n                hero.hp = hero.max_hp\n                hero.atk += 2\n                print(f"  🆙 레벨 업! Lv.{hero.level} (HP:{hero.max_hp} ATK:{hero.atk})")\n            return True\n        \n        # 몬스터 공격\n        dmg = monster.attack(hero)\n        print(f"  💥 {monster.name}의 공격! {dmg} 데미지!")\n        \n        if not hero.is_alive():\n            print(f"\\n  💀 {hero.name} 쓰러짐...")\n            return False\n    return hero.is_alive()\n\n# 게임 실행\nhero = Character("용사", 100, 15, 5)\nmonsters = [\n    ("🐗 멧돼지", 30, 8, 2),\n    ("🐺 늑대", 45, 12, 3),\n    ("🐉 드래곤", 80, 18, 8),\n]\n\nprint("🏰 텍스트 RPG")\nprint("=" * 35)\n\nfor name, hp, atk, df in monsters:\n    monster = Character(name, hp, atk, df)\n    if not battle(hero, monster):\n        print("\\n💀 게임 오버...")\n        break\n    hero.heal(30)\n    print(f"\\n  💊 체력 회복! {hero.status()}")\nelse:\n    print("\\n🏆 모든 몬스터를 물리쳤습니다! 축하합니다!")\n\nprint(f"\\n📊 최종 상태: Lv.{hero.level} | EXP: {hero.exp}")\n\`\`\`\n\n## 🎯 도전 과제\n1. 스킬 시스템을 추가하세요\n2. 상점에서 아이템을 구매하는 기능을 추가하세요\n3. 보스 몬스터와 특별 패턴을 추가하세요` },
        { order: 5, title: "코드 품질과 리팩토링", type: "lesson", content: `# ✨ 코드 품질과 리팩토링\n\n좋은 코드를 작성하기 위한 원칙들을 배웁니다.\n\n## PEP 8 스타일 가이드\n\`\`\`python\n# ❌ 나쁜 스타일\nx=1;y=2;z=x+y\ndef f(a,b):return a+b\n\n# ✅ 좋은 스타일\nx = 1\ny = 2\nz = x + y\n\ndef add(a, b):\n    return a + b\n\`\`\`\n\n## 이름 짓기 규칙\n\`\`\`python\n# 변수, 함수: snake_case\nstudent_name = "홍길동"\ndef calculate_average(scores):\n    return sum(scores) / len(scores)\n\n# 상수: UPPER_SNAKE_CASE\nMAX_RETRY = 3\nPI = 3.14159\n\n# 클래스: PascalCase (CamelCase)\nclass StudentRecord:\n    pass\n\`\`\`\n\n## DRY 원칙 (Don't Repeat Yourself)\n\`\`\`python\n# ❌ 반복 코드\nprint(f"홍길동: {85+92+78}점, 평균: {(85+92+78)/3:.1f}")\nprint(f"김철수: {90+88+95}점, 평균: {(90+88+95)/3:.1f}")\n\n# ✅ 함수로 추출\ndef show_score(name, scores):\n    total = sum(scores)\n    avg = total / len(scores)\n    print(f"{name}: {total}점, 평균: {avg:.1f}")\n\nshow_score("홍길동", [85, 92, 78])\nshow_score("김철수", [90, 88, 95])\n\`\`\`\n\n## 독스트링 (Docstring)\n\`\`\`python\ndef calculate_bmi(weight, height_cm):\n    \"\"\"BMI를 계산합니다.\n    \n    Args:\n        weight: 체중 (kg)\n        height_cm: 키 (cm)\n    \n    Returns:\n        BMI 값 (float)\n    \"\"\"\n    height_m = height_cm / 100\n    return weight / (height_m ** 2)\n\`\`\`` },
        { order: 6, title: "다음 단계 안내", type: "lesson", content: `# 🚀 다음 단계 안내\n\n축하합니다! 11차시 과정을 모두 완료했습니다! 🎉\n\n## 📊 배운 내용 정리\n\n| 차시 | 주제 | 핵심 키워드 |\n|------|------|-------------|\n| 1 | 파이썬 첫 만남 | print, 주석, 에러 |\n| 2 | 변수와 자료형 | 변수, int/float/str/bool |\n| 3 | 연산자와 수식 | 산술/비교/논리 연산자 |\n| 4 | 조건문 | if, elif, else |\n| 5 | 반복문 | for, while, break |\n| 6 | 리스트 | 리스트, 컴프리헨션 |\n| 7 | 딕셔너리/튜플 | dict, tuple, set |\n| 8 | 함수 | def, return, lambda |\n| 9 | 문자열 | 메서드, 포맷팅, 정규식 |\n| 10 | 파일/예외처리 | try-except, JSON, 모듈 |\n| 11 | 종합 프로젝트 | 설계, 구현, 리팩토링 |\n\n## 🎯 다음에 배울 것들\n1. **객체지향 프로그래밍 (OOP)** - 클래스와 상속\n2. **웹 프로그래밍** - Flask, Django\n3. **데이터 분석** - Pandas, NumPy, Matplotlib\n4. **인공지능** - scikit-learn, TensorFlow\n5. **게임 개발** - Pygame\n6. **자동화** - 업무 자동화 스크립트\n\n## 💡 학습 팁\n- 매일 조금씩 코딩하세요 (30분이면 충분!)\n- 프로젝트를 직접 만들어 보세요\n- 에러를 두려워하지 마세요\n- 다른 사람의 코드를 읽어보세요\n- 커뮤니티에 참여하세요\n\n## 🏆 수료를 축하합니다!\n파이썬의 기초를 탄탄히 다졌습니다. 이제 여러분은 프로그래머입니다! 🐍✨` },
    ],
    quizzes: [
        { question: "좋은 변수 이름 규칙은?", options: JSON.stringify(["camelCase", "snake_case", "PascalCase", "UPPER_CASE"]), answer: 1, order: 1 },
        { question: "DRY 원칙의 의미는?", options: JSON.stringify(["코드를 말리기", "반복하지 않기", "빠르게 작성하기", "주석 달기"]), answer: 1, order: 2 },
        { question: "이 과정에서 총 몇 가지 자료형을 배웠나요?", options: JSON.stringify(["3가지", "5가지", "7가지", "10가지"]), answer: 1, order: 3 },
    ],
};
